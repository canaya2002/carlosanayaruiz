/node_modules
/.next/
/.next-build/
/.next-dev/
/out/
/build
.DS_Store
*.pem
npm-debug.log*

# Local env files. .env.example IS committed — it documents the contract.
.env
.env.local
.env*.local
!.env.example

.vercel
*.tsbuildinfo
next-env.d.ts

# Capturas de scripts/shoot.mjs. Se escriben en la raíz porque son de un solo
# uso — se miran y se tiran. Versionarlas engorda el repo con megabytes que
# quedan obsoletos en el siguiente cambio de diseño.
/*-desktop.png
/*-tablet.png
/*-movil.png
/*-hero.png
/*-nav.png

# Portadas del blog SIN optimizar (214 MB). Las publicadas viven en
# public/blog/ como WebP de ~57 kB; estas son el material de origen y no
# tienen por qué viajar ni al repo ni al despliegue. Ver scripts/blog-covers.mjs
/assets/blog-covers-originales/
